<Window x:Class="AbleToTrack.Views.AlertView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" Title="AlertView" Height="300" Width="400" WindowStyle="None"
        WindowStartupLocation="CenterScreen">
    <Border Background="LightGray" CornerRadius="20" Padding="5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock x:Name="TextBlockMessage" TextAlignment="Center" VerticalAlignment="Center" />
            <Button x:Name="ButtonClose" Click="ButtonClose_OnClick"  Content="Close" Grid.Row="1" HorizontalAlignment="Center" />
        </Grid>
    </Border>
</Window>
